/**
 * ⚠️ DEPRECATED — DO NOT IMPORT.
 *
 * This component was used by the old /admin/complaints page before it was
 * rewritten as a full management console with assign/status/photo support.
 *
 * The new canonical implementation lives in:
 *   src/app/admin/complaints/page.tsx
 *
 * Kept as a stub only because file deletion isn't permitted.
 */
export default function ComplaintTable() {
  return null;
}
